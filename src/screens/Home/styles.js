import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        backgroundColor: '#001',
        flex: 1,
        paddingHorizontal: 5
    },
    list: {
        marginVertical: 10
    }
});